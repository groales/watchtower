# ============================================
# CONFIGURACIÓN DE WATCHTOWER
# ============================================

# Zona horaria
TZ=Europe/Madrid

# ============================================
# CONFIGURACIÓN VÍA COMMAND (docker-compose.yaml)
# ============================================
# Las siguientes opciones están configuradas en docker-compose.yaml
# mediante argumentos --flag y NO deben definirse aquí:
#
# - WATCHTOWER_CLEANUP (configurado: --cleanup)
# - WATCHTOWER_SCHEDULE (configurado: --schedule=0 30 3 * * *)
# - WATCHTOWER_LABEL_ENABLE (configurado: --label-enable)
# - WATCHTOWER_INCLUDE_STOPPED (por defecto: false)

# ============================================
# NOTIFICACIONES
# ============================================

# Activar notificaciones vía Shoutrrr
WATCHTOWER_NOTIFICATIONS=shoutrrr

# URL de notificación SMTP (puerto 25, sin autenticación)
# Formato: smtp://SMTP_HOST:25/?from=REMITENTE&to=DESTINATARIO&subject=ASUNTO
# Ejemplo con servidor local o relay sin auth:
WATCHTOWER_NOTIFICATION_URL=smtp://mail.midominio.com:25/?from=noreply@midominio.com&to=admin@midominio.com&subject=Watchtower%20-%20Actualizaciones

# Nivel de notificaciones (panic, fatal, error, warn, info, debug, trace)
WATCHTOWER_NOTIFIER_LEVEL=info

# Enviar reporte final con resumen de actualizaciones
WATCHTOWER_NOTIFICATIONS_REPORT=true

# ============================================
# EJEMPLOS ADICIONALES
# ============================================

# Múltiples destinatarios (separar por comas):
# WATCHTOWER_NOTIFICATION_URL=smtp://mail.midominio.com:25/?from=noreply@midominio.com&to=admin@midominio.com,devops@midominio.com&subject=Watchtower%20-%20Actualizaciones

# Múltiples canales de notificación (separar por punto y coma):
# WATCHTOWER_NOTIFICATION_URL=smtp://mail.midominio.com:25/?from=noreply@midominio.com&to=admin@midominio.com;telegram://TOKEN@telegram?channels=CHAT_ID
